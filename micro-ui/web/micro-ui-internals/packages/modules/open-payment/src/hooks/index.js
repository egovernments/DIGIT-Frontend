import utils from "../utils";

const openpayment = {
 
};

const Hooks = {
  openpayment,
};

const Utils = {
  browser: {
    sample: () => {},
  },
  openpayment:{
    ...utils
  }
};

export const CustomisedHooks = {
  Hooks,
  Utils,
};
