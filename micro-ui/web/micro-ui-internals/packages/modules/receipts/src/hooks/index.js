import utils from "../utils";

const RECEIPTS = {
};

const Hooks = {
  RECEIPTS
};

const Utils = {
  browser: {
    DSS: () => { },
  },
  RECEIPTS: {
    ...utils,
  },
};

export const CustomisedHooks = {
  Hooks,
  Utils,
};
